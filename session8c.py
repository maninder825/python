names = "John, jennie, Jim, John, Jack, joe"
splittedNames = names.split(",")
print(splittedNames, type(splittedNames))

for names in splittedNames:
    print(names.strip())


quotes = """work hard, get luckier
search the candles, rather than cursing the darknes
"""

words = quotes.split(" ")
print(words)
for word in words:
    print(word)

    # HW
def split(sentence):

    words=[]

    return words()

split("Search the Candle, rather than cursing the darkness")